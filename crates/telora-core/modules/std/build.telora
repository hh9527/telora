type TextFile = struct {
    path: String,
    content: String,
};
type Artifact = enum {
    'TextFile(TextFile),
};
type OutputPlan = struct {
    files: Array(Artifact),
};
export {
    TextFile,
    Artifact,
    OutputPlan,
};
