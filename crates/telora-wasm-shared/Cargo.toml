[package]
name = "telora-wasm-shared"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1", default-features = false, features = ["alloc"] }
serde_json = { version = "1", default-features = false, features = ["alloc", "raw_value"] }
