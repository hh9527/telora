# #193：保留类型信息的失配实验

分支：`fix/193-directional-fit`，基于 `5f481180`。当前是实验原型，不能视为 #193 已完成或可合入。

## 实验范围

将两种情况拆开：

- 不能形成类型的错误仍可将槽标为 Conflicted，例如结构递归、未解析引用等。
- 两个已存在结构类型不匹配时，记录失败约束及两侧槽、位置、诊断，但不因此抹掉两侧类型。

父结构在子结构比较没有产生新冲突后才合并，避免提前合并后丢失实际类型。这仍是在原图上操作，没有复制环境、逐模块隔离或失败后回滚。

注意：原型落在共享的 equal 结构比较路径，因此影响不止 Fit；这不是已经完成来源区分的定向契约求解器。尤其不能将“某个槽目前已有结构”直接等同于“用户声明的刚性契约”。

## 已验证的改善

独立语言资产位于 `tests/language/src/check/diag-shared-contract/` 和 `diag-contract-local/`，内部状态由 `type_resolve/tests/contracts.rs` 检查。

| 观察 | main | 实验分支 |
| --- | --- | --- |
| 错误调用后的共享 increment 签名 | Conflicted | 保留 Fn(Int) -> Int |
| 健康模块的 answer | Conflicted | 保留 Int |
| 连续传入 String 和 Bool 的两次错误调用 | 仅第一次报告 | 两次分别报告 |
| 输入字符串变量 | 可能沿共享约束污染 | 保留 String |
| 失败 MIR 的 seal | 拒绝 | 仍拒绝，清除诊断文本后也拒绝 |

新断言还验证了：交换导入顺序、同模块泛型错用与正常实例共存、合法未注解调用继续推导，以及函数体违反已声明返回类型仍然失败。

## 回归暴露的问题

以下完整回归结果来自加入 RFC 0297 顶层签名检查之前，不能视为当前工作树的全量结果。

没有通过修改旧预期将回归强行变绿：

- 两项 core 测试和一项 CLI query 测试依赖“失配后值类型必为 Conflicted”的旧约定。原型保留已知类型，但还没有给 query 增加对应的使用合法性信息。
- `@check` 的诊断上下文目前通过槽上的 Conflicted ID 找到原冲突。原型去掉结构失配的污染后，该关联丢失；一项 core 测试和十个语言负例的文案断言失败。这十个语言负例仍全部退出失败、记录冲突，没有误放行。
- 本轮完整语言回归未发现健康用例失败；新增内部状态测试通过。未做本实验的 release 性能评估。

## 下一步

目前已按新决策将契约检查扩展到所有顶层普通值，局部绑定继续推导。MIR 保存
DeclarationContract 义务，seal 重新校验；签名缺失不能因为实现已推导出 Known 而绕过。
`decl/def` 配对复用 decl 的注解，类型成员导入沿用既有身份。当前四项 core 定向测试
（包含内置标准库全图求解及 seal）通过；批量静态诊断的 CLI 用例通过，module 指向
primary 所属规范模块，session 保存根选择。失败约束已直接保存诊断索引，不再靠
文案反查。@check 与失败约束的关联尚未恢复，完整性仍需独立于值证据求解验证。

全仓测试资产和生成源码尚未迁移，新语言检查预计会暴露大量缺失签名；本阶段未重新
运行完整回归，也没有性能结论。

收益已可观察，但需要把失败约束本身变为明确、可查询的证据：保存发生失配的使用节点、契约来源及关联上下文，使“已知类型”与“这处使用是否通过检查”分开表达。随后恢复 @check 的精确上下文，审计部分未知槽与真正共享推导变量的冲突语义，再推进所有 Fit 入口和批量诊断归因。

保持 #193 打开。不能仅调整旧断言、增加 isolate 或静默忽略错误来宣称完成。
