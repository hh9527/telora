data data = import(json) "./abc.json";
use self::User as User;
use std::json as json;

def user: User::Type = (data |> User::decode).unwrap!();
def text: String = user |> User::encode |> json::stringify_pretty(2);
pub use self::{ text as output };
