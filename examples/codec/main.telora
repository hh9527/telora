import "./abc.json" as data;
import "./User.telora" as User;
import "core:result" as result;
import "core:json" as json;

def user = data |> User.decode |> result.unwrap;
def text = user |> User.encode |> json.stringify_pretty(2);
export { text as output };
