import "./b2b-model.telora" { compile };
import "./restricted.json" { data as restriction };

export def output = compile(restriction, {
    measures: ['NetRevenue],
    alignment: 'Natural,
    dimensions: ['OrderMonth, 'CustomerRegion],
    filters: ['CustomerRegion("East")],
    order_by: ['OrderMonth],
    limit: 'None,
    render: {mode: 'Table, fields: ["order_month", "customer_region", "net_revenue_cents"]},
});
