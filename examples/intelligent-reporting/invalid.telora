import "./b2b-model.telora" { compile };
import "./restriction.json" { data as restriction };

export def output = compile(restriction, {
    measures: ['NetRevenue],
    alignment: 'Natural,
    dimensions: ['ProductCategory, 'EmployeeKind, 'ProductSku],
    filters: [],
    order_by: ['CustomerRegion],
    limit: 'Some(20),
    render: {mode: 'Table, fields: ["missing_render_field"]},
});
