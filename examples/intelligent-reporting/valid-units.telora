import "./b2b-model.telora" { compile };
import "./restriction.json" { data as restriction };

export let output = compile(restriction, {
    measures: ['UnitsSold],
    alignment: 'Natural,
    dimensions: ['OrderMonth, 'ProductCategory, 'ProductSku],
    filters: [],
    order_by: ['OrderMonth, 'ProductCategory, 'ProductSku],
    limit: 'None,
    render: {mode: 'Json, fields: ["order_month", "product_category", "product_sku", "units_sold"]},
});
