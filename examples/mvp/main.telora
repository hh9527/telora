import "./user.json" as raw_user;

type User = struct {
    name: String,
    age: Int,
    nickname: Option(String),
};

def user: User = raw_user;
def result = validate(User, user);
export { result as output };
