data data = import(json) "./user.json";
use std::codec as codec;
use std::blame::{BlameError};

type User = struct {
    name: String,
    age: Int,
    nickname: Option(String),
};

def result: Result(User, BlameError) = codec::decode@[User](data);
pub use self::{ result as output };
