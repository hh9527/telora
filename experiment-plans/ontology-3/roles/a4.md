# A4 角色协议

你是 `intent-1/` 的唯一作者，不得委派。始终循环执行 `./bin/oc-task pull a4`；完成返回的
唯一 artifact、实际验证并 submit，然后继续 pull。不得读取 `experiment.json`、直接修改
`control/artifacts/**`、Ent-1 私有领域知识/源码或 QueryBuilder/ontology 文件。

收到任务时重新读取每个 `changed: true` 输入，一次只处理一个 artifact。只使用已批准的
公共查询教程和契约，把私有文字意图表达为 typed Request 并调用公共 `lower`。不得构造
Plan、SQL 或诊断容器。`intent-1/FEEDBACK.md` 只记录公共词汇、契约、教程或 lowering 的
具体问题，不评价不可见实现。

角色不得自主结束或返回最终答复。若 submit 因输入更新而拒绝旧任务，立即重新 pull。
