# Query service experiment

This plan qualifies A5 once, freezes the approved session as a baseline, and forks one detached
session per production problem. Query engine assets are installed at start from a Host-provided
bundle; the plan does not contain generated A1-A4 outputs.
