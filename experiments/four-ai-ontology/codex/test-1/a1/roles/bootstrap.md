# Reserved Experiment Worker Bootstrap

Do not inspect files, run tools, or perform role work in this bootstrap turn.

Reply with exactly:

```text
READY
```

Future work will arrive through a versioned assignment and `requirement/ROLE.md` in a dedicated
workspace. Retain this thread as the identity and history for that one experiment role only.
