# Intent Notes

The requested order count maps directly to the public `OrdersCreated` measure. The requested
grouping uses the public `OriginRegion` and `CarrierName` dimensions. No unavailable or inferred
concepts are needed.
