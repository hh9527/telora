# Intent

The public intent requests `OrdersCreated` grouped by `ProductCategory` exactly
as specified. Both identifiers are members of the published closed
vocabularies. The public compiler remains responsible for validating the
order-to-product grain expansion and deciding whether a plan can be published.
