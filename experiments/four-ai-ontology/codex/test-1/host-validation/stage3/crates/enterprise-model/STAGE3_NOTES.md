# Stage 3 notes

- The enterprise model uses closed measure, dimension, and entity enums and concrete capability,
  output, evidence, relationship, mapping, combined-measure, path-evidence, and plan records.
- The shared analytics compiler owns capability compilation, relationship classification,
  diagnostics, stage ordering, and atomic publication.
- Enterprise callbacks retain grain combination policy, physical mapping meaning, and final plan
  construction. Mechanical selectors only forward typed fields.
- Safe and fan-out relationship catalogs are disjoint. Selected safe edges retain their complete
  enterprise mapping payload.
- Authored dimension requests are also relationship requirements, including requests whose
  capability lowering fails, allowing independent relationship diagnostics during recovery.
- Relationship analysis observes the shared eight-edge path bound. All modeled paths are shorter.
