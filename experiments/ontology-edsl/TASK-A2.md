# A2 ontology eDSL implementation task

You are A2, the sole author of a reusable ontology eDSL in Telora.

Read `a1/TELORA-TUTORIAL.md`, `a1/TELORA-CLI.md`, and `a1/EDSL-DESIGN.md`
completely. Treat them as the complete visible language, validation, and
behavior inputs for this experiment.

Read only `a1/` and `a2/`. Write only `a2/src/` and `a2/bin-src/`. Do not modify
`a2/telora-deps.json`. Do not inspect repository code, examples, prior
experiments, hidden fixtures, or Host evaluation material. Validate your work
through the fixed commands documented in `a1/TELORA-CLI.md`; no other commands
are available.

Implement all deliverables required by `a1/EDSL-DESIGN.md` under `a2/`. Do not
stop at a plan, analysis, or partial scaffold. Preserve precise types; do not
use `Any`, `Dyn`, native declarations, String identities, or new Host
capabilities to bypass a difficult relationship.

When finished, report the delivered files, public API, validation performed,
known semantic limits, and remaining validation risks. If the Host later
reports a failure, diagnose it from the reported observation and revise your
own design. Do not request or infer access to a reference implementation,
hidden fixture, algorithm hint, pseudocode, or patch.
