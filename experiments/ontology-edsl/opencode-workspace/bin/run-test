#!/usr/bin/env bash
set -euo pipefail

if [[ $# -ne 0 ]]; then
    printf 'usage: ./bin/run-test\n' >&2
    exit 64
fi

workspace=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd -P)
exec "$workspace/bin/telora" run "$workspace/a2/bin-src/test.telora"
